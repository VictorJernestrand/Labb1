@model IEnumerable<Labb1.Models.Product>

<h1>Produkter</h1>


@*<p>
    <a asp-action="Create">Create New</a>
</p>*@
<div id="product-detail"class="row d-flex flex-row justify-content-around align-content-around flex-wrap">
    @foreach (var item in Model)
    {
    <div class="card flex-grow-0 flex-shrink-1-m" style="position: relative; margin-bottom:20px">
        <div class="row no-gutters">
            <div class="col-auto">
                <div class="black-tooltip" data-toggle="tooltip" title="@item.Description"><img class="thumbnail" style="background-image: url('@item.ImageURL')"></div>
            </div>
                <div class="col" style="max-width: 150px; min-width: 150px">
                    <div class="card-block px-2" style="max-width: 175px; min-width: 175px">
                        <h4 class="card-title">@item.Name</h4>
                        <div class="d-inline-block" style="position: absolute; bottom: 0; margin-left:0px">
                            <p style="display:flex">
                                @item.Price.ToString("C0")
                                <span >
                                    <a style="margin-left: 5px" asp-controller="Product" asp-action="ProductDetail" asp-route-id="@item.Id"><i class="fas fa-info-circle"></i></a>
                                </span>
                                <span @*class="float-right btn btn-outline-success"*@ style="float:right; margin-left: 20px;">
                                
                                    <button id="buyBtn" class="btn btn-success " onclick="AddToCart('@item.Id')" @*asp-controller="Product" asp-action="AddToCart" asp-route-id="@item.Id"*@><span class="button-text">Köp</span></button>
                                   
                                </span>
                            </p>
                        </div>
                        </div>
                    </div>
        </div>
    </div>
        
    }
    </div>







    @*<table class="table">
            <thead>
                <tr>
                    <th>
                        Produkt
                    </th>
                    <th>
                        Beskrivning
                    </th>
                    <th>
                        Pris
                    </th>
                    <th>
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
        @foreach (var item in Model) {
                <tr>
                    <td>
                        @item.Name
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Description)
                    </td>
                    <td>
                        @item.Price.ToString("C0")
                    </td>
                    <td>
                        <img class="product-image" src="@item.ImageURL" alt="@item.Name" />
                        <div class="thumbnail" style="background-image: url('@item.ImageURL');">
                        </div>
                    </td>
                    <td>
                        @Html.ActionLink("Lägg till i varukorg", "AddToCart", "Products", new { id = item.Id })
                        <button class="btn add-to-cart" onclick="AddToCart(@item.Id)">Köp skiten!</button>
                        <a asp-controller="Product" asp-action="AddToCart" asp-route-id="@item.Id">Köp</a>
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                        @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                        @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                    </td>
                </tr>
        }
            </tbody>
        </table>*@
