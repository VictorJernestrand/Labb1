@using Microsoft.AspNetCore.Identity
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

<ul class="navbar-nav">
    @if (SignInManager.IsSignedIn(User))
    {
        <div id="user-buttons">
            <li class="nav-item">
                <a id="logIn" class="btn btn-outline-info log-btn buttons" asp-area="Identity" asp-page="/Account/Manage/Index">Profil@*@User.Identity.Name*@</a>
            </li>
            <li class="nav-item">
                <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                    <button id="logOut" type="submit" class="btn btn-outline-info log-btn buttons">Logga ut</button>
                </form>
            </li>
        </div>
    }
    else
    {
        <div id="user-buttons">
            <li class="nav-item">
                <a id="logIn" class="btn btn-outline-info log-btn buttons" asp-area="Identity" asp-page="/Account/Login">Logga in</a>
            </li>
            <li class="nav-item">
                <a class="btn btn-outline-info log-btn buttons" asp-area="Identity" asp-page="/Account/Register">Registrera</a>
            </li>
        </div>
    }
</ul>
